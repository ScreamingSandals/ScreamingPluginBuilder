gradlePlugin {
    plugins {
        pluginBuilder {
            id = 'org.screamingsandals.plugin-slib'
            implementationClass = 'org.screamingsandals.gradle.slib.SLibPlugin'
        }
    }
}